import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    detail: {
        fontWeight: "bold",
        fontSize: 17,
        color: 'white',
        marginTop: 10
    },
    detailss: {
        color: 'white',
        fontSize: 17,
        fontWeight: 'regular',
        marginTop: 10

    }
})
export default styles;